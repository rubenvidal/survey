<%= form_for @encuesta do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </p>
  <% f.fields_for :preguntas do |builder| %>
    <%= render 'pregunta_fields', :f => builder %>
  <% end %>
  <p><%= f.submit %></p>
<% end %>
